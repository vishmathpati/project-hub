# STATUS.md

Current session: compatibility expansion for Claude Code, Claude Desktop, Codex CLI, and Codex Desktop.

## State

- Project Hub is a SwiftPM macOS app.
- Existing code already has project discovery, skill scanning/install, agents, MCP import/copy/edit, and settings screens.
- This worktree was missing the project-protocol canon, so it has been bootstrapped from README.md, ROADMAP.md, and source inspection.
- Current focus has been narrowed to Claude Code, Claude Desktop, Codex CLI, and Codex Desktop; Roo/Cline/other editor compatibility is parked for later.
- Claude Code WebSocket MCP configs (`type = "ws"` / `wss://...`) are now accepted as Claude-Code-supported, shown as manual `/mcp` verification in MCP Health, and still left unsupported for Codex/non-Claude targets.
- Codex Desktop skill support now includes the official admin skill root `/etc/codex/skills`, matching Codex CLI visibility.
- Claude Desktop MCPB/extension archives and installed extension scans now accept host-managed `server.type = "uv"` manifests without `server.mcp_config`, surface them as app-managed runtime, and still report required `user_config` values.
- Codex skill path copy in Settings and Skills empty states now includes `/etc/codex/skills` as an admin read-only root alongside `~/.agents/skills` and managed/legacy `~/.codex/skills`.
- Claude Desktop account-managed skills are now explicit in the Compatibility matrix as an app/account surface with no local filesystem write target.
- Claude Code settings `permissions.additionalDirectories` is no longer treated as a skills root; Project Hub keeps it as file-access/context evidence instead of falsely reporting skills from those directories.
- Claude Code filesystem skill rows now reflect live-watched existing skill directories rather than blanket restart-required semantics.
- Codex project-local ignored-setting detection now includes `apps_mcp_product_sku`.
- Codex CLI profile config files (`$CODEX_HOME/<profile>.config.toml`) are now discovered for the runtime profile picker, included as selected/default profile MCP/settings surfaces, and treated as top-level config layers for Codex CLI plugin MCP policy without affecting Codex Desktop.
- Codex CLI profile config files now also layer into CLI-only instruction fallback/model-instruction settings, plugin install/enablement discovery, and `[[skills.config]]` skill overrides; Codex Desktop keeps using the non-profile config layers.
- Codex plugin MCP policy fixes targeting a profile config file now edit top-level `[plugins...]` policy inside that file instead of writing legacy nested `[profiles.<name>.plugins...]` tables.
- The normal Global MCP Codex inventory now includes the default `profile = "<name>"` profile file for direct MCP servers and plugin install/policy discovery, so Global MCP no longer silently misses profile-file servers that Compatibility can see.
- The normal Global MCP Claude Desktop inventory now includes installed Claude Desktop extension MCP servers as read-only rows, resolving `${__dirname}` and `${user_config.*}` from local extension manifests/settings so the MCP manager no longer hides extension-owned servers that Compatibility can see.
- Normal MCP inventory now has a parser-only Compatibility overlay for Claude/Codex MCP surfaces: Global MCP, Project MCP, and Live Mode context estimation can show Compatibility-discovered read-only rows while existing direct `ConfigWriter` project rows remain writable and duplicate baseline rows are suppressed.
- Global MCP health now resolves read-only Compatibility/plugin/extension rows against the row's owning `sourcePath` instead of the tool's canonical config file, while writable rows still use the normal tool config path. Claude Desktop extension live verification stays conservative and does not launch extension-owned commands from Project Hub.
- Compatibility now shows a next-step card for the active scan lens, routing users toward the first previewable file fix, manual app-owned follow-up, live MCP Verify, or rescan so the core scan -> explain -> fix -> verify workflow is explicit.
- Compatibility live Verify now replaces the stale static "handshake not yet verified" finding with any real live result, including broken/needs-auth/restart states, and manual app-owned fixes now include a Codex Desktop launch handoff alongside Claude Desktop.
- MCP import now has Claude/Codex quick target presets for All, CLI, Desktop, and Project before the diff preview, selecting only writable Claude Code, Claude Desktop, and Codex targets that match the chosen scope and existing safety blockers.
- MCP import CLI preset copy now matches the actual write policy: Codex global MCP is writable, while Claude Code global MCP is a Claude CLI handoff because Project Hub does not write private `~/.claude.json` global MCP state.
- MCP import apply now writes the exact approved batch preview with `ConfigWriter.applyTextPreview`, so changed-on-disk target configs are refused instead of silently recomputing and overwriting after the user reviewed a diff.
- MCP copy now shows the destination file diff before applying global or project copies, uses the approved preview text with stale-file protection per destination, and reports per-target preview/stale failures instead of only aggregate counts.
- Copy/readback now handles disabled JSON MCP source rows more safely: `ConfigWriter.readServer` falls back to `<key>_disabled` maps after active maps miss, preserving raw disabled config before the copy sheet's narrower scanned-row fallback is needed.
- The active MCP/skills/project UX is now constrained to the Claude/Codex primary set: Global MCP reads only Claude Desktop, Claude Code, and Codex; project detection ignores editor-only markers; project MCP lists only Claude/Codex surfaces; profile copy no longer exposes Cursor rules or editor MCP files; and Settings/Project detail no longer advertise editor-only skill/rule workflows.
- Profile Copy now executes that narrowed policy too: copy options/results no longer carry Cursor Rules, project profile MCP copy migrates only Claude `.mcp.json` and Codex `.codex/config.toml`, and nested Claude/Codex skill roots remain copyable without false warnings.
- Active Project MCP, Copy MCP, and Projects helper fallbacks now label only Claude/Codex project config targets; dormant legacy editor readers/views remain unmounted from the primary workflow.
- Project MCP and Hooks active surfaces now share the same Claude/Codex scope: the Project MCP tab derives project tools from `ToolSpecs.projectScopedTools`, and Hooks reads only Claude Code plus Codex hooks so hidden editor-only data cannot create empty rendered sections.
- Codex Hooks now follow the documented active layers in the Project tab: Project Hub reads global `CODEX_HOME/hooks.json`, inline `[hooks]` from `CODEX_HOME/config.toml`, and trusted project `.codex/hooks.json` plus `.codex/config.toml` layers.
- Claude Code global MCP writes are now blocked as CLI/manual-only because they target private `~/.claude.json` state; Project Hub still writes Claude Code project `.mcp.json` when Project scope is selected.
- `ToolSpecs` now honors the same Claude path overrides as scanners for `PROJECTHUB_CLAUDE_JSON_PATH` and `PROJECTHUB_CLAUDE_DESKTOP_SUPPORT_DIR`.
- Codex SQLite project discovery now keeps non-git folders when they have Claude/Codex markers instead of requiring `.git`.
- Claude Code auth scanning now runs `claude auth status` first when available, treats OS credential storage/API key helpers as runtime-managed, detects documented CLI auth-source environment variables by source name only when CLI status is unavailable, and only uses auth-shaped `~/.claude.json` fields as redacted legacy/inferred evidence. Claude Desktop account auth remains app/account-runtime-managed with no local secret inspection.
- Codex auth scanning now treats `CODEX_ACCESS_TOKEN` and `OPENAI_API_KEY` as redacted login inputs rather than proof of stored auth, flags placeholder/empty env credentials as needs-auth, treats credentialless `auth.json` as needs-auth, and keeps `mcp_oauth_credentials_store` evidence separate from main Codex login.
- `CODEX_HOME` resolution is now centralized through `ProjectHubPaths`: blank/whitespace env values fall back to `~/.codex`, tilde paths expand and standardize, and scanner/reader/writer/project/skill/Compatibility fix-plan paths share the same behavior.
- Compatibility now includes `~/.claude.json` as a read-only Claude Code user global config/runtime surface, separate from user MCP and per-project local state, so top-level private config/cache keys are visible without becoming write targets.
- Current compatibility work now includes Codex plugin MCP default-profile policy: user-level `profile = "<name>"` makes matching `profiles.<name>.plugins.<plugin>.mcp_servers.<server>.enabled` effective for read-only inventory and Compatibility fixes, while profile policies without an explicit default profile remain evidence-only.
- The selected user profile now stays above trusted project plugin MCP policy in Compatibility, and Global MCP plugin-bundled rows explain that they are read-only because they point into installed plugin files.
- Codex plugin MCP enable fixes now clear the active profile disablement plus inherited top-level disablement when both are needed, treat `CODEX_HOME/config.toml` as global even when `CODEX_HOME` is a `.codex` directory, and keep project `.codex/config.toml` profile sections ignored for effective policy.
- Non-default Codex profile-scoped plugin MCP disablement now appears as conditional Unknown/settings evidence that names the relevant `codex --profile <name>` runtime path instead of marking the server disabled in the current static inventory.
- Compatibility scans now have an internal Codex CLI runtime-profile selection model: when Project Hub has explicit evidence for `codex --profile <name>`, that profile's plugin MCP disable policy can apply to Codex CLI while Codex Desktop and ordinary static scans keep the policy conditional.
- The Compatibility UI now exposes that model as a compact Codex CLI profile picker sourced from user-level `CODEX_HOME/config.toml`; copied reports persist the profile used by the scan.
- Global MCP Codex plugin-bundled rows now expose previewed enable/disable actions that write Codex plugin MCP policy in the owning user config instead of editing installed plugin bundle files; copy/edit/remove stay read-only for those bundle-owned rows.
- Compatibility now keeps source-aware Codex plugin MCP policy candidates, so user/global, trusted project, and active-profile disagreements produce a conflict finding that names the effective layer and the shadowed layer, with a previewed cleanup for the stale lower-precedence `enabled` policy.
- Codex plugin MCP policy writes now have apply-time stale-preview revalidation: Global MCP and Compatibility write the exact approved diff only when the target config still matches the preview, and profile-scoped disabled findings carry explicit profile metadata for runtime/default profile fixes.
- Generic previewed Codex TOML settings repairs now share the same apply-time stale-preview guard: approval, sandbox, web/network, permission-network, filesystem glob-depth, and workspace-root cleanups apply the exact approved diff only when the target config still matches the preview.
- Invalid Codex filesystem permission-rule findings now offer conservative previewed repairs for top-level dotted keys, section keys, one-level inline filesystem tables, and nested one-line inline `:workspace_roots` tables when a valid nested sibling remains, preserving valid `read`/`write`/`deny` rules.
- Invalid Codex network proxy and named permission-network domain/Unix-socket rule findings now offer conservative previewed repairs for exact rule sections, parent dotted rule entries, one-level inline rule maps, and nested one-line inline rule maps while preserving valid sibling rules.
- Compatibility scanning no longer repeatedly runs full project-root detection while matching Claude local project state, and ProjectRootDetector honors `PROJECTHUB_CLAUDE_JSON_PATH` so tests and app-internal scans can isolate Claude project records from the user's live `~/.claude.json`.
- The non-Compatibility Project MCP reader now shares Claude project MCP approval-state reading with Project tab/context estimation: `.mcp.json`, Claude user/project/local settings, managed settings/drop-ins, and private `~/.claude.json` project records can mark project MCP servers disabled, while Project tab enable actions are blocked when the owning disable source is outside `.mcp.json`.
- Project-tab and context estimation now include Claude Code local/private project MCP servers stored under `~/.claude.json.projects[projectRoot].mcpServers`, with a distinct `claude-code-local` source identity so same-named `.mcp.json` and private rows do not collide. Private rows display as read-only and hide toggle/edit/remove actions.
- Compatibility project MCP scans now reuse the same Claude project approval-state reader as the Project tab, so `.mcp.json`, Claude settings, managed settings/drop-ins, and private project records cannot drift between scan results and project inventory.
- Claude Code MCP servers named `workspace` are now flagged as a reserved-name broken state because Claude Code skips that name at load time; the check is Claude-Code-only, so Codex servers named `workspace` remain valid. Project Hub also refuses preview/import/copy writes that would create a new Claude Code `workspace` MCP server and explains the reserved-name reason in the import diff preview.
- Compatibility issue sheets and copied Markdown reports now include compact applicability context that names the matched surface, scope, runtime/profile, owning path, write ownership, and restart/rescan expectation before a user previews, follows, or shares a fix.
- Codex `project_doc_max_bytes = 0` findings now remember the exact effective config layer that disabled project instructions and offer a stale-preview-guarded repair when that layer is safely writable.
- Deprecated Codex `experimental_instructions_file` findings now offer a safe preview: Project Hub renames the top-level key to `model_instructions_file` when no replacement exists, or removes only the deprecated duplicate when the supported key is already present.
- Missing Codex configured instruction-file findings now remember the owning top-level config key and config path, appear in targeted Manual Actions, and offer a stale-preview-guarded removal for stale `model_instructions_file` / `experimental_instructions_file` overrides when the owning TOML layer is safely writable.
- MCP health preflight now reports missing environment references in stdio launch arguments and remote URL templates before live Verify expands them to empty strings or malformed URLs; `${VAR:-fallback}` remains accepted.
- Claude Code stdio MCP health now follows documented startup semantics for `.mcp.json`: `command` env-template fallbacks are expanded before executable lookup, missing command env vars report as needs-auth/setup instead of broken command paths, and live Verify honors `MCP_TIMEOUT` when no per-server timeout is configured.
- VS Code-style MCP `${input:...}` placeholders and `envFile` / `env_file` dependencies are now preserved through inventory/import/health/scan paths and reported as prompt-backed input/auth or app-specific env-file setup instead of being mistaken for ordinary Claude/Codex environment variables.
- MCP `cwd` / working-directory settings are now preserved through inventory/import/scan/health paths, missing working directories are reported as broken setup, and live stdio Verify launches relative commands from the configured cwd instead of the Project Hub process directory.
- VS Code MCP sandbox/dev metadata is now preserved for VS Code inventory/imports, reported as app-specific runtime/security evidence in Compatibility, and stripped from Claude/Codex writes so Project Hub does not imply VS Code sandbox enforcement carries across tools.
- Roo Code MCP tool-control/runtime metadata (`alwaysAllow`, `disabledTools`, `watchPaths`, `timeout`, inline `disabled`) is now preserved for Roo inventory/imports, reported as app-specific behavior in Compatibility, written back in Roo's native shape, and stripped or mapped safely when copying to Claude/Codex.
- Claude Code remote MCP configs with `headersHelper` are preserved as dynamic auth evidence, including plugin-bundled inventory rows, reported as conservative Unknown instead of false missing-OAuth/auth, and live Verify does not execute helper shell commands.
- Claude Code remote MCP configs with nested `oauth` metadata from official `add-json`/OAuth flows are preserved as app-owned OAuth evidence, suppress hosted-OAuth false positives, and live Verify returns conservative Unknown instead of probing the endpoint without the target tool's OAuth credentials.
- App-owned MCP auth/runtime evidence now has dedicated Compatibility issue types and manual guidance: headersHelper and OAuth metadata are target-tool-managed auth follow-ups, while Claude Desktop MCPB/DXT extension runtime/configuration is app-managed runtime follow-up. Live Verify remains conservative and does not execute helpers, read secrets, or impersonate OAuth flows.
- Cross-tool MCP writes now normalize remote auth into the target app's config shape: Codex `bearer_token_env_var`, `http_headers`, and `env_http_headers` become Claude/JSON `headers` on JSON writes and previews, while Codex TOML writes preserve Codex-native auth keys.
- GitHub repository import discovery now scans nested MCP config files, not just nested `server.json`, so nested `mcp.json`, `mcp-fetch.json`, `.mcp.json`, Claude Desktop config files, and editor MCP config paths can be found before README fallback.
- GitHub repository import discovery now includes Roo project MCP config candidates at `.roo/mcp.json`, and JSON import parsing accepts snake_case `mcp_servers` wrappers used by some real-world MCP configs.
- Codex import discovery now treats `.codex/config.toml` as an importable repo/tree/archive config, handles quoted-root `["mcp_servers"."name"]` TOML sections, and scans deeper source-archive candidate lists so earlier invalid config files do not hide a later valid Codex MCP config.
- Official MCP Registry `server.json` imports now support `registryType = "mcpb"` package entries as Claude Desktop extension archive handoff choices, preserving the artifact URL and `fileSha256` metadata and verifying SHA-256 before previewing downloaded archives.
- MCP From URL import now supports direct remote archive downloads: `.zip`, `.tar.gz`, `.tgz`, `.mcpb`, `.dxt`, GitHub `/archive/...`, and GitHub `/releases/download/...` assets are downloaded to a unique temp file and passed through the same local archive preview path. GitHub release listing pages remain guided so Project Hub does not treat HTML pages as archives.
- Raw `raw.githubusercontent.com` import URLs for known MCP config/README paths now route through GitHub content fetch instead of paste-mode remote-server import, preserving the pasted raw URL and adding existing Contents API fallback candidates for slash-ref branches.
- GitHub `github.com/<owner>/<repo>/raw/<ref>/<path>` import URLs now route through the same GitHub content-fetch path as blob/raw.githubusercontent config URLs, so raw route MCP config links are imported as files instead of generic remote endpoints.
- Paste-mode gist/pastebin/raw config-document URLs now show From URL guidance instead of becoming fake hosted MCP endpoints, while real hosted `/mcp` endpoints still import as remote servers.
- Codex requirements policy findings remain inspect-only but now have targeted Manual Actions guidance for filesystem `deny_read` and MCP `identity` policy issues; copied reports include finding subject paths so policy owners can see the exact key to change.
- Codex plugin MCP policy writes now parse single-quoted TOML dotted section segments, so global and profile-scoped policies with quoted plugin IDs, profile names, or server names containing dots are edited in place without duplicate double-quoted policy tables.
- Direct Codex MCP TOML handling now has the same quoted-section parity: `[mcp_servers.'github.docs']` reads as `github.docs`, previews write dotted IDs as quoted TOML keys, toggles update existing quoted sections instead of appending malformed duplicates, remove deletes nested subtables for the same server, and Compatibility does not split quoted names into subtables.
- Direct Codex MCP enable/disable toggles now edit only the parent section `enabled` assignment in place, preserving user-authored nested TOML subtables such as `[mcp_servers.foo.env]`, `[mcp_servers.foo.headers]`, and `[mcp_servers.foo.env_http_headers]` instead of flattening them into inline tables during a toggle.
- Project-tab Codex MCP inventory now normalizes `headers`, `http_headers`, `env_http_headers`, bearer-token env vars, and startup/tool timeouts from project `.codex/config.toml`, including quoted dotted server IDs, nested header subtables, dotted header keys, and trailing TOML comments.
- Global and Project-tab Codex MCP inventory now share quoted-root TOML and local-env semantics: `["mcp_servers"."github.docs"]` reads as `github.docs`, and `env_vars` entries with `source = "remote"` are not reported as missing local credentials.
- Codex skill compatibility now preserves official OpenAI skill app metadata (`icon_small`, `icon_large`, `brand_color`, `default_prompt`) and treats quoted `[["skills"."config"]]` TOML override tables like `[[skills.config]]` for scan and previewed enable repairs.
- Codex project tables now share quoted-section parity with MCP/skills: `[projects."/repo.with.dot"]`, `["projects"."/repo.with.dot"]`, and single-quoted variants are recognized for root detection, trust/settings scan, summaries, and trust previews/writes without appending duplicate project sections.
- The normal Skills tab and Live Mode context estimate now use a shared Compatibility-backed skill inventory: Claude parent/nested/additional-directory skills, Codex selected-working-directory and parent `.agents/skills`, Codex `[[skills.config]]` disabled state, duplicate same-name origins, and read-only/plugin/managed skill sources stay visible outside the Compatibility tab. Skills tab removal now targets only the selected writable origin instead of deleting both Claude and Codex copies by name, and global install buttons target the matching primary tool family instead of copying every skill to both tool roots.
- The normal Skills tab now carries Compatibility duplicate-name and version-conflict diagnostics into installed skill rows, so broken/conflicting skill state is visible outside the Compatibility scan.
- Profile Copier now uses detected source/target project roots for project profile migration, copies writable inventory-backed Claude/Codex skill origins, preserves nested/quoted Codex MCP TOML server sections, includes VS Code/Roo project MCP JSON scopes, and warns when copied skills may still depend on additional-directory loading or disabled/limited policy state.
- Compatibility/import UX now better matches the scan/explain/fix/verify workflow: the main tab is labeled Compat, import diffs show by default, project-scope imports no longer offer unsafe global credential writes, next-step copy names desktop reload versus fresh CLI sessions, and stale Claude/Codex skill path labels now distinguish `~/.agents/skills` from managed/legacy `~/.codex/skills`.
- Compatibility now has an explicit target preset picker for All Supported, Project, Global, CLI, Desktop, and Custom lenses. Presets drive the existing project/global scan target plus scope/runtime filters, and copied Markdown reports include the selected target preset.
- Compatibility now has a visible Tool Coverage rollup for Claude Code, Claude Desktop, Codex CLI, and Codex Desktop in the main scan flow. The copied Markdown report includes a four-tool coverage matrix plus richer detailed matrix columns for file-control, safe writes, disable support, OAuth/auth, env expansion, restart, precedence, paths, and notes.
- MCP import now handles prompt-driven wizard installers as a user-driven flow: commands like `npx @vendor/wizard mcp add` are blocked from ordinary npx/uv command import, shown in a dedicated handoff card, and can be copied for the user to run manually before refreshing Project Hub.
- MCP import now keeps that prompt-driven installer handoff visible as a secondary path when README/source-archive content also contains safe import snippets; safe config/command choices remain preferred, and Project Hub still does not execute prompt-driven installers.
- Prompt-driven installer detection now covers more runner shapes, including `px mcp add`, `npm exec ... mcp add`, `pnpm dlx ... mcp add`, `bun ... mcp add`, `bunx ... mcp add`, `uvx ... mcp add`, `pipx run ... mcp add`, and unconfigured direct `mcp add`, while parseable direct/tool-owned MCP add commands remain safe import choices.
- GitHub README-only wizard installers and direct fetched README/raw URLs now use the same prompt-driven installer handoff instead of becoming fake launch-command imports or generic "no config found" failures.
- Direct non-GitHub fetched README/raw URLs now also enter the import choice/preview flow immediately for one or multiple safe snippets, instead of only filling the raw text box and waiting for a separate Next step.
- Local/downloaded source archives now also use the prompt-driven installer handoff when a README contains only a wizard installer, while still preferring safe config/import choices when present.
- Vendor MCP config documents named `mcp_settings.json` or `cline_mcp_settings.json` are now recognized across pasted URLs, GitHub candidates, raw GitHub URLs, and source archives, preventing those JSON config links from being misclassified as hosted MCP endpoints.
- VS Code-style `inputs` metadata in `mcp.json` now feeds import credential requirements for env, header, and URL placeholders, including prompt descriptions/password flags, so next-step credential prompts explain `${input:...}` values before writing.
- Compatibility now separates safe file fixes from manual work: previewable fixes appear before Manual Actions, findings show Preview fix vs Manual badges, Manual Actions excludes issue-derived items with safe previewed fixes, and copied Markdown reports include a Previewable Fixes section.
- Compatibility previewed settings fixes now apply the exact approved before/after preview for the remaining stale-sensitive Codex/Claude config repairs instead of recomputing writer output at apply time, including invalid settings cleanup, instruction-limit repairs, project trust/overlap cleanup, ignored project settings, Codex skill overrides, and Claude MCP approval conflicts.
- MCP health and Compatibility auth scans now understand editor-style `${env:VAR}` templates in commands, args, env maps, URLs, and headers, expand them during health checks, report missing variables as `needs-auth`, and treat empty environment values consistently as missing.
- MCP Health now rejects unsupported transports such as `websocket` as broken before attempting remote verification, matching Compatibility's static transport findings.
- Codex MCP Compatibility now flags `${VAR}` / `${env:VAR}` inside literal Codex `env` and `http_headers` values even when Project Hub's own environment has the variable set, steering users toward `env_vars`, `env_http_headers`, `bearer_token_env_var`, explicit values, or wrapper scripts as appropriate. Env-file path variables are now preflighted before missing-file/conversion warnings so unresolved `${env:...}` paths produce one specific needs-auth finding.
- Direct Codex MCP tool controls now preserve `enabled_tools`, `disabled_tools`, `default_tools_approval_mode`, and per-tool `tools.<tool>.approval_mode` through global/project inventory, writer round-trips, health entries, and Compatibility scans without mislabeling Codex-native `disabled_tools` as Roo-specific metadata.
- Docker MCP launch auth is now detected from `docker run --env` / `-e` flags even when imported configs do not also carry an `env` map, so missing host credentials show as needs-auth in MCP Health and Compatibility.
- Claude Desktop remote MCP imports and writes now respect the official connector boundary: remote/hosted servers are not offered for `claude_desktop_config.json`, the import UI points users to Settings > Connectors, and `ConfigWriter` defensively blocks programmatic remote writes while still allowing local stdio servers.
- Copy-to-apps now uses the same write blockers as import/preview/write paths, so blocked targets such as Claude Desktop hosted/remote MCPs are disabled and explained before the user applies a copy.
- Hosted OAuth MCP auth detection now covers official GitHub Copilot, Notion, Atlassian, and Supabase hosted endpoints in both MCP Health and Compatibility static scans.
- Docker MCP launch preflight now covers `--env-file`/`--env-file=` credential files plus bind/volume host paths, reporting missing env files as needs-auth/setup and missing mounts as broken paths.
- Docker MCP command imports now preserve `--env-file` / `--env-file=` as `envFile` metadata while leaving the original Docker launch args untouched.
- Docker MCP JSON/server-manifest imports now also preserve `--env-file` metadata and Docker `--env` credential hints when configs use `command` arrays or `command: "docker"` plus `args`.
- Docker structured `--mount` parsing in MCP Health and Compatibility now tolerates duplicate fields without crashing, and MCP Health classifies `${input:...}` placeholders in `envFile` and `cwd` before file/directory lookup.
- Normal MCP inventory now normalizes JSON command arrays and safe command strings into `command`/`args` for global plugin rows, project server entries, and Claude local project-state detail rows, so ordinary MCP tabs match Compatibility's launch parsing.
- Docker path placeholders now preflight before filesystem lookup: missing `${env:...}` variables in `--env-file` / mount paths show Docker path-variable auth issues, and `${input:...}` mount prompts avoid misleading missing-path findings.
- MCP command import naming now recognizes `npm exec`, `npm x`, `pnpm dlx`, and `uv run --with` package forms while preserving the wrapper command and full args unchanged.
- MCP health no longer lets placeholder OAuth metadata such as `your-client-id` suppress hosted OAuth needs-auth classification.
- Docker MCP mount checks now distinguish Docker-managed named volumes from explicit bind host paths: `-v name:/data` and `--mount type=volume,source=name` do not produce host-path errors, while structured bind mounts such as `--mount type=bind,source=relative-name` are checked as relative host paths with case-insensitive mount type parsing.
- Compatibility auth scans now match MCP Health's OAuth placeholder behavior: placeholder-only OAuth metadata such as `your-client-id` no longer appears as app-managed OAuth and no longer suppresses hosted MCP OAuth-required findings.
- Official Homebrew MCP command-string configs are now normalized consistently: `command = "brew mcp-server"` / `"command": "brew mcp-server"` splits to command `brew` and args `["mcp-server"]` across import, global/project inventory, health, and Compatibility paths.
- Rust and Go source MCP launch configs are now normalized consistently: `command = "cargo run --release"` and `"command": "go run ./cmd/server"` split into launcher commands plus args across import, inventory, health, writer, and Compatibility paths.
- Codex remote MCP writes now preserve `env_file` dependencies, so copied hosted/remote servers keep the same env-file setup evidence that MCP Health and Compatibility inspect.
- Copied Compatibility reports now use server IDs for live Verify rows and show verified health in the MCP Servers table, so Scan > Verify > Copy preserves the post-verify result.
- Ordinary global/project MCP inventory now ignores placeholder-only OAuth metadata such as `your-client-id` and `...`, keeping inventory rows aligned with MCP Health and Compatibility auth classification.
- Compatibility Verify now only enables for directly verifiable MCP servers; connector/extension-only lenses explain that app-managed connectors/extensions must be checked in the owning app.
- Malformed Codex plugin MCP policy key/value findings now carry exact section/key repair metadata and offer a stale-preview-guarded fix that removes only the invalid key from the owning TOML section.
- OAuth hint-only metadata such as callback ports, callback URLs, redirect URIs, and scopes no longer makes placeholder configs look app-managed. Real OAuth identifiers/metadata still preserve those hints in inventory, writes, Health, and Compatibility.
- MCP Health and Compatibility now expand `${userHome}` in cwd, envFile, Docker env-file, and Docker bind paths before reporting missing directories/files.
- Generic Compatibility MCP enable/remove fixes now apply the exact approved file preview with stale-preview refusal instead of recomputing the mutation at apply time.
- `/usr/bin/env` MCP command wrappers now unwrap to the real launcher in imports, MCP Health, and Compatibility scans, preserving wrapper env hints while allowing Docker env-file/mount and package-runner diagnostics to run against the actual command.
- `/usr/bin/env -S` / `--split-string` MCP wrappers now unwrap in imports, MCP Health, and Compatibility scans, with env `-i` / `-u` tolerated before the actual launcher.
- Project discovery now detects Claude/Codex skills, instruction files, and project settings markers, so skills-only or instructions-only folders are not missed as AI-tool projects.
- Codex TOML MCP writes now normalize Claude-style `Authorization = "Bearer ${TOKEN_ENV}"` remote headers into `bearer_token_env_var` instead of writing a Codex literal header that will not expand.
- Normal MCP inventory/readback now shares env-wrapper launch normalization for display metadata: global JSON/TOML inventory, project `readAllServerEntries`, and Claude local project rows show the effective launcher/env/envFile while `readServer` keeps the raw authored config for edit/writeback safety.
- Codex config-backed project discovery now uses the same structural TOML section parser as root detection, so `[projects."/repo"]`, `["projects"."/repo.with.dot"]`, and single-quoted variants do not drift between detection and the project picker; Claude/Codex skills-only folders are also root markers for both ProjectStore and Compatibility scans.
- Codex plugin-bundled skills are now visible in Compatibility and the normal Skills inventory: installed `.codex-plugin/plugin.json` `skills` roots produce read-only Codex CLI/Desktop skill surfaces, plugin-disabled skills are reported as disabled, and invalid/missing plugin skill paths are surfaced from the manifest.
- MCP Registry remote/package imports now preserve import-only credential requirement metadata for headers, URL variables, and package env vars without writing Project Hub metadata into target config files; the import next-step credential prompt uses that planner so header and URL placeholder env names are no longer invisible after import.
- MCP Registry credential parsing now accepts object-map `headers` and `environmentVariables` shapes in addition to array entries, so real-world manifests do not silently lose header/env credential prompts.
- MCP Registry URL variable parsing now accepts scalar object-map values for remote and package HTTP transports, resolving static defaults like `{region}` without creating fake credential prompts while still preserving `${TENANT_ID}` placeholder requirements.
- MCP Registry input parsing now resolves nested `variables` inside headers, environment variables, and package arguments, so values like `{weather_choices}` become real placeholders/defaults instead of literal braces; import next steps also skip static env defaults such as `LOG_LEVEL=debug`.
- Import next-step credential saves now use typed credential requirements instead of plain env hints: Codex remote URL placeholders are resolved, header placeholders are written as literal `http_headers` when users paste values, existing literal headers are preserved, opencode local env writes use `environment`, and Claude Desktop remote entries keep the connector write boundary.
- MCP import preview now surfaces typed credential needs before install: choice cards and editable server rows show env, header, and URL-value names from the shared credential planner without exposing placeholder values or blocking safe imports.
- Claude Desktop recent MCP log diagnostics now distinguish expired login/token signals from generic auth-needed errors, producing `Auth expired` compatibility findings and server health while preserving secret/email/URL redaction.
- Compatibility auth scans now treat existing auth JSON files with empty or placeholder credential-looking fields as `needs-auth`, reporting only the redacted field path and never the stored value.
- Compatibility auth scans no longer treat OAuth `expires_in`/`*_expires_in` lifetime fields as absolute Unix timestamps, preventing valid short token lifetimes such as `3600` from becoming false expired-auth findings.

## Known Risks

- MCP/skill config behavior changes frequently across Claude and Codex; verify against official docs before shipping logic.
- Config writes must remain previewable and backed up.
- The current TOML parser/writer is lightweight and may not preserve every advanced Codex table shape.

## Next Actions

- Continue compatibility dogfooding against real Claude/Codex/vendor MCP configs.
- Continue hardening MCP Registry and vendor manifest imports against more real-world package/input shapes.
- Keep expanding safe preview/apply settings fixes only when the owning config layer is explicit, especially remaining structured settings repairs and real-world direct MCP dogfooding.
- Continue verifying code changes with focused tests plus `swift build` and app bundle packaging.

## Validation

Current validation: focused ConfigWriter disabled-map read regression passed with 1 test at 2026-05-31 15:19 IST, and touched-file trailing-whitespace scan passed.

Earlier validation: `swift build` passed at 2026-05-31 15:15 IST after the MCP copy approved-preview apply patch, and touched-file trailing-whitespace scan passed.

Earlier validation: `swift build` passed at 2026-05-31 15:10 IST after the MCP import approved-preview apply patch, and touched-file trailing-whitespace scan passed.

Earlier validation: `swift build` passed at 2026-05-31 15:06 IST after the MCP import quick-target preset patch, and touched-file trailing-whitespace scan passed.

Earlier validation: `swift build` passed at 2026-05-31 15:02 IST after the Compatibility next-step/live-Verify UX patch, and touched-file trailing-whitespace scan passed.

Earlier validation: `swift build` passed at 2026-05-31 15:00 IST after the Compatibility next-step UX patch, and touched-file trailing-whitespace scan passed.

Earlier validation: focused MCPStore source-path health regression slice passed with 2 tests at 2026-05-31 14:54 IST, and touched-file trailing-whitespace scan passed.

Earlier validation: focused MCP inventory adapter regression slice passed with 3 tests at 2026-05-31 14:48 IST, `swift build` passed, and touched-file trailing-whitespace scan passed.

Earlier validation: focused FullConfigReader Claude Desktop extension inventory regression passed at 2026-05-31 14:40 IST, and touched-file trailing-whitespace scan passed.

Earlier validation: focused FullConfigReader Codex default profile-file inventory and Claude Code `~/.claude.json` global-config regression slice passed with 2 tests at 2026-05-31 14:33 IST, and touched-file trailing-whitespace scan passed.

Earlier validation: focused FullConfigReader Codex default profile-file inventory regression passed at 2026-05-31 14:30 IST, and touched-file trailing-whitespace scan passed.

Earlier validation: focused Codex profile-file instruction/skill/plugin/writer regression slice passed with 5 tests at 2026-05-31 14:25 IST, and touched-file trailing-whitespace scan passed.

Earlier validation: focused Codex profile-file/plugin MCP policy regression slice passed with 3 tests at 2026-05-31 14:12 IST, and touched-file trailing-whitespace scan passed.

Earlier validation: focused Claude/Codex skill and ignored-settings regression slice passed with 4 tests at 2026-05-31 14:03 IST, and `git diff --check` passed.

Earlier validation: focused Claude Desktop UV MCPB archive/extension regression slice passed with 5 tests at 2026-05-31 13:53 IST, and `git diff --check` passed.

Earlier validation: focused Claude Code WebSocket MCP and Codex Desktop admin skill visibility regression slice passed with 6 tests at 2026-05-31 13:43 IST, and `git diff --check` passed.

Earlier validation: focused VS Code input import, vendor config filename discovery, credential-save, and MCP Health unsupported-transport regression slice passed with 8 tests at 2026-05-31 13:28 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Registry nested input-variable and import credential prompt/save regression slice passed with 6 tests at 2026-05-31 13:20 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Registry scalar URL-variable regression slice passed with 4 tests at 2026-05-31 13:12 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Registry object-map credential input regression slice passed with 4 tests at 2026-05-31 13:06 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Codex auth-expiry/auth-placeholder regression slice passed with 2 tests at 2026-05-30 19:12 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Codex auth-placeholder regression passed at 2026-05-30 19:07 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Claude Desktop expired-auth log compatibility test passed at 2026-05-30 19:06 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused ImportCredentialPlanner tests passed with 2 tests at 2026-05-30 18:58 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused credential/import tests passed with 3 tests at 2026-05-30 18:56 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Registry credential parser/planner tests passed with 2 tests at 2026-05-30 18:39 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Codex plugin skill Compatibility/Inventory tests passed with 3 tests at 2026-05-30 18:33 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused ProjectRootDetector tests passed with 4 tests at 2026-05-30 18:25 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused env-split inventory/readback tests passed with 4 tests at 2026-05-30 18:17 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused env-split/import/health/compatibility, ProjectStore marker detection, and Codex bearer-header write normalization tests passed with 7 tests at 2026-05-28 23:49 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused stale-preview and `/usr/bin/env` wrapper tests passed with 9 tests at 2026-05-28 23:36 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused OAuth hint-only and `${userHome}` path-placeholder tests passed with 10 tests at 2026-05-28 23:22 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused OAuth inventory and Codex plugin policy metadata/fix-plumbing tests passed with 3 tests at 2026-05-28 23:13 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused cargo/go command-string plus Codex remote env_file tests passed with 3 tests at 2026-05-28 23:06 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Homebrew MCP command-string import/inventory tests passed with 2 tests at 2026-05-28 22:59 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Compatibility OAuth placeholder tests passed with 3 tests at 2026-05-28 22:56 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Docker structured-mount tests passed with 3 tests at 2026-05-28 22:48 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Docker named-volume/bind tests passed with 3 tests at 2026-05-28 22:45 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused npm/uv import and hosted OAuth placeholder tests passed with 7 tests at 2026-05-28 22:39 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused command-array inventory and Docker path-placeholder tests passed with 6 tests at 2026-05-28 22:33 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: focused Docker JSON import, duplicate structured-mount, and prompt-backed envFile/cwd health tests passed at 2026-05-28 22:23 IST after SwiftPM cache escalation, and `git diff --check` passed.

Earlier validation: `swift test --filter 'ImportParserCommandTests/testDockerEnvFileAddsLaunchEnvFileHint|ImportParserCommandTests/testDockerEnvFileInlineFlagAddsLaunchEnvFileHint|ImportParserCommandTests/testDockerEnvFlagAddsLaunchEnvHint'` passed with 3 tests and `git diff --check` passed at 2026-05-28 22:13 IST after Docker env-file import preservation.

Earlier validation: `swift test --filter 'MCPHealthCheckerTests/testEvaluateDockerEnvFileNeedsExistingFile|MCPHealthCheckerTests/testEvaluateDockerBindMountNeedsExistingHostPath|CompatibilityMCPAuthTests/testDockerEnvFileAndMountPathsAreReportedMissing|MCPHealthCheckerTests/testEvaluateKnownHostedOAuthProvidersNeedLogin|CompatibilityMCPAuthTests/testKnownHostedOAuthProvidersAreReportedNeedsAuthWithoutPATHeader'` passed with 5 tests and `git diff --check` passed at 2026-05-28 22:09 IST after hosted OAuth provider and Docker env-file/mount preflight work.

Earlier validation: `swift test --filter 'MCPImportScopePlannerTests|ConfigWriterProjectScopeTests/testClaudeDesktopRemoteMCPIsRejectedBeforeWrite|ConfigWriterProjectScopeTests/testClaudeDesktopStdioMCPIsNotConnectorBlocked'` passed with 4 tests at 2026-05-28 21:51 IST after connector-aware Claude Desktop remote MCP import/write blocking. `swift test --filter 'MCPHealthCheckerTests/testEvaluateDockerEnvFlagNeedsHostEnv|CompatibilityMCPAuthTests/testDockerEnvFlagsAreReportedMissing'` passed with 2 tests at 2026-05-28 21:48 IST after Docker MCP env-flag auth detection.

Current validation: `swift test --filter 'FullConfigReaderTests/testCodexTOMLMCPReaderPreservesDirectToolControls|ConfigWriterProjectScopeTests/testCodexProjectWritePreservesDirectToolControls|CompatibilityMCPAuthTests/testCodexDirectToolControlsAreNotReportedAsRooSpecific'` passed with 3 tests and `git diff --check` passed at 2026-05-28 21:42 IST after direct Codex MCP tool-control compatibility.

Current validation: `swift test --filter 'MCPHealthCheckerTests/testEvaluateEnvFilePathEditorEnvTemplateNeedsAuthBeforeFileLookup|CompatibilityMCPAuthTests/testEnvFilePathEnvTemplateIsReportedWithoutGenericEnvFileNoise|CompatibilityMCPAuthTests/testCodexLiteralEnvTemplatesAreReportedEvenWhenEnvironmentIsSet'` passed with 3 tests and `git diff --check` passed at 2026-05-28 21:28 IST after Codex literal env-template and envFile path-variable compatibility.

Current validation: `swift test --filter 'MCPHealthCheckerTests/testEvaluateStdioLaunchArgEditorEnvTemplateNeedsAuth|MCPHealthCheckerTests/testEvaluateStdioCommandEditorEnvTemplateNeedsAuthBeforeCommandLookup|CompatibilityMCPAuthTests/testEditorEnvTemplatesAreReportedMissing'` passed with 3 tests and `git diff --check` passed at 2026-05-28 21:19 IST after editor-style MCP env-template compatibility.

Current validation: `swift test --filter 'ConfigWriterProjectScopeTests/testRooToolControlMetadataIsOnlyWrittenBackToRooTargets|ImportParserJSONTests/testVSCodeEnvFileIsPreservedAndNormalized|CompatibilityMCPAuthTests/testRooToolControlMetadataIsReportedAsAppSpecificRuntime'` passed with 3 tests and `git diff --check` passed at 2026-05-28 21:11 IST after Roo MCP tool-control/runtime compatibility.

Current validation: `swift test --filter 'ConfigWriterProjectScopeTests/testVSCodeSandboxDevMetadataIsOnlyWrittenBackToVSCodeTargets|ConfigWriterProjectScopeTests/testVSCodeProjectInventoryPreservesEnvFile|ImportParserJSONTests/testVSCodeEnvFileIsPreservedAndNormalized|CompatibilityMCPAuthTests/testVSCodeSandboxAndDevModeAreReportedAsAppSpecificRuntime'` passed with 4 tests and `git diff --check` passed at 2026-05-25 18:00 IST after VS Code MCP sandbox/dev compatibility.

Current validation: `swift test --filter 'MCPHealthCheckerTests/testVerifyStdioUsesConfiguredCWDForRelativeCommand|MCPHealthCheckerTests/testEvaluateMissingCWDIsBroken|CompatibilityMCPAuthTests/testMissingMCPWorkingDirectoryIsReportedBroken|ConfigWriterProjectScopeTests/testVSCodeProjectInventoryPreservesEnvFile|ImportParserJSONTests/testVSCodeEnvFileIsPreservedAndNormalized|FullConfigReaderTests/testCodexGlobalInventoryIncludesConfiguredPluginMCPServersAsReadOnly'` passed with 6 tests and `git diff --check` passed at 2026-05-25 17:49 IST after MCP working-directory compatibility.

Last validation: `swift test --filter 'MCPHealthCheckerTests/testEvaluateEnvFileNeedsAuthAndReportsMissingFile|CompatibilityMCPAuthTests/testEnvFileIsReportedAsAppSpecificMCPAuthSetup|ConfigWriterProjectScopeTests/testVSCodeProjectInventoryPreservesEnvFile|ImportParserJSONTests/testVSCodeEnvFileIsPreservedAndNormalized'` passed with 4 tests and `git diff --check` passed at 2026-05-25 17:38 IST after VS Code MCP envFile compatibility. Earlier `swift test --filter 'MCPHealthCheckerTests/testEvaluateInputVariablePromptNeedsAuthBeforeLaunch|CompatibilityMCPAuthTests/testInputVariablePlaceholdersAreReportedAsPromptBackedAuth'` passed with 2 tests and `git diff --check` passed at 2026-05-25 12:57 IST after input-variable placeholder classification. Earlier `swift test --filter 'SkillInventoryReaderTests|ProjectRootDetectorTests|ConfigWriterSettingsRepairTests/testPreviewSetCodexProjectTrustUpdatesQuotedRootProjectTable|CompatibilityContextSettingsTests/testCodexQuotedRootProjectSectionTrustIsReported'` passed with 23 tests and `git diff --check` passed at 2026-05-25 12:50 IST after Skills diagnostics and quoted Codex project-section parity. Earlier `swift test --filter 'ImportParserRegistryManifestTests|ImportParserArchiveTests'` passed with 26 tests and `git diff --check` passed at 2026-05-25 12:38 IST after MCP Registry MCPB package handoff support. Earlier `swift test --filter ImportParserCommandTests` passed with 17 tests and `git diff --check` passed at 2026-05-25 10:24 IST after broad prompt-driven MCP installer detection. Earlier `swift test --filter 'ImportParserCommandTests|ImportParserGitHubURLTests|ImportParserArchiveTests'` passed with 74 tests and `git diff --check` passed at 2026-05-25 10:17 IST after mixed safe-snippet plus prompt-driven installer import handling. Earlier `swift build`, `swift test --filter 'CompatibilityContextSettingsTests|ConfigWriterSettingsRepairTests|CompatibilitySkillSupportTests'` with 147 tests, and `git diff --check` passed at 2026-05-25 10:11 IST after stale-preview apply hardening for Compatibility settings fixes. Earlier `swift test --filter 'ImportParserCommandTests|ImportParserGitHubURLTests|ImportParserArchiveTests'` passed with 72 tests and `git diff --check` passed at 2026-05-25 10:04 IST after fetched URL/import-choice and archive handoff work. Earlier `swift test --filter ImportParserArchiveTests` passed with 14 tests and `git diff --check` passed at 2026-05-25 10:02 IST after source archive prompt-driven installer handoff. Earlier `swift test --filter ImportParserGitHubURLTests` passed with 43 tests and `git diff --check` passed at 2026-05-25 09:58 IST after fetched README/raw URL prompt-driven installer handoff. Earlier `swift build`, `swift test --filter CompatibilityReportMarkdownTests` with 2 tests, and `git diff --check` passed at 2026-05-25 09:53 IST after Previewable Fixes visibility. Earlier `swift test --filter ImportParserCommandTests` passed with 15 tests, `swift build` passed, and `git diff --check` passed at 2026-05-25 09:48 IST after prompt-driven installer handoff. Longer focused tests were intentionally deferred for bulk completion speed. Earlier `swift build` and `git diff --check` passed at 2026-05-25 09:42 IST after the Tool Coverage rollup and report matrix expansion. Earlier `swift build` and `git diff --check` passed at 2026-05-25 09:37 IST after the Compatibility target preset picker. Earlier `swift build` and `git diff --check` passed at 2026-05-25 09:32 IST after app-owned MCP auth/runtime guidance. Earlier `swift build` and `git diff --check` passed at 2026-05-25 09:26 IST after direct remote archive import support. Earlier `swift build` and `git diff --check` passed at 2026-05-25 09:21 IST after the bulk Profile Copier / Compatibility UX patch. Earlier `swift test --filter SkillInventoryReaderTests` passed with 4 tests at 2026-05-25 02:18 IST, and `swift test --filter 'SkillInventoryReaderTests|CompatibilitySkillSupportTests|CompatibilityContextSettingsTests'` passed with 91 tests at 2026-05-25 02:15 IST. `swift build`, `bash build-app.sh`, `plutil -lint ProjectHub.app/Contents/Info.plist`, `git diff --check`, and debug scans passed at 2026-05-25 02:16 IST, with a final `git diff --check` pass at 2026-05-25 02:18 IST. The debug scan only matched existing debug-log guidance and the existing `fingerprint` helper references. Earlier `swift test --filter 'ProjectMCPReaderTests|CompatibilityProjectMCPTests|ConfigWriterProjectScopeTests'` passed with 49 tests at 2026-05-25 02:01 IST, MCP config/auth/import/read-write gate passed with 89 tests at 2026-05-25 01:50 IST, MCP health/auth/project/import/write gate passed with 82 tests at 2026-05-25 01:40 IST, and OAuth health-entry gate passed with 86 tests at 2026-05-25 01:32 IST.
