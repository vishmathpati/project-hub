# agents/docs/detail/

Deep-dive documentation for features too complex for a single dependency map entry.

## Creation Rule

Only create a file here when a feature's flow cannot be captured in the `agents/docs/INDEX.md` entry.

- DO create: multi-step config migration, full MCP writer format contract, Live Mode watcher lifecycle.
- DO NOT create: basic SwiftUI view notes, simple CRUD-like list rendering, standard sheet behavior.

## Files

(none yet — created as needed)
