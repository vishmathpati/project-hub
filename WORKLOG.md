# Worklog — Project Hub
> Cleared after each session.

- 2026-08-18 · Grok: ran 5-agent research (existing 12 tools, new CLIs, IDEs, shared standards, in-app audit). Two real shared specs: AGENTS.md + SKILL.md. MCP is a path/key adapter, not a spec. Hub UI is first-class only for Claude Code / Desktop / Codex; 9 MCP readers are hidden; Cursor Rules CRUD is unwired; CLAUDE.md editor misses official variants; AGENTS.md has no editor; Roo/Continue/Cline/Windsurf MCP paths are stale. New local-file candidates: Amp, Factory Droid, Copilot CLI, Kiro, Qwen, Crush, Goose, Warp, Junie, Augment. Awaiting user filter on providers + features.
- 2026-08-18 · Grok: locked primary-provider + performance research. Command Code = commandcode.ai. Google canonical = Antigravity (Gemini leftover files stay as legacy reader). Pi MCP is community extension (pi-mcp-adapter) + ~/.pi/agent/mcp.json, not core. Scan killer: CompatibilityScanner full-tree enumerator (no skip of large dirs) + in-memory-only results. Worktree bug: Project.canonicalize walks to main/configured root; worktrees are hidden. Usage: copy ccusage/codeburn local session files only, not quota scrapers. Rust stack change not started — needs permission; Swift scan/cache first.
- 2026-08-18 · Grok: stack decision — stay Swift/SwiftUI. No Rust backend, no launchd service. Performance fix is known-path reads + persist cache + split the 13k CompatibilityScanner so each page loads only what it needs. Revisit Rust only if a measured Swift indexer still stalls after that.
- 2026-08-18 · Grok: first Swift scan patch. Replaced CompatibilityScanner full-tree skill enumerator with KnownSkillRoots (official `.claude/skills` only, skip node_modules/.build, no symlink follow). Compat/Plugins last scan now saved under Application Support/ProjectHub/scan-cache. Worktree rows can be added as their own project; addDiscovered removes them from the related list. Tests are temp-dir only. Isolated KnownSkillRoots driver passed; full swift test blocked (no Xcode, SwiftUI macros missing).
- 2026-08-18 · Grok: wrote current-build ROADMAP.md as the single progress list. Added instruction-file picker (AGENTS.md / CLAUDE variants / GEMINI.md), remounted Cursor Rules, scanned ~/.cursor/skills, unhid Cursor/VS Code/OpenCode/Zed MCP, added Antigravity/Pi/Command Code MCP path adapters, and skill copy across Claude/Codex/Cursor folders. Remaining: scanner split, copy skill to another project, usage.
- 2026-08-18 · Grok: copy one skill to another project; added Usage tab that totals local JSONL tokens for Claude, Codex, Pi, Command Code. Remaining open item: split the 13k CompatibilityScanner.
- 2026-08-18 · Grok: built this checkout with /Applications/Xcode-beta.app and replaced /Applications/ProjectHub.app (v0.3.0, ad-hoc signed). Launched it for user testing.
- 2026-08-18 · Grok: rebuilt and reinstalled /Applications/ProjectHub.app using Xcode-beta 27.0 (27A5237l) as requested. App launched.
- 2026-08-18 · Grok: user was right — providers were hidden in MCP readers, not a Providers page. Added Providers sidebar with 10 tools + controls. Fixed Codex usage (payload.info.total_token_usage, newest files, last snapshot). Reinstalled /Applications/ProjectHub.app.
- 2026-08-18 · Grok: Providers Controls now lists skills/MCP/instructions with Open, Copy to project, Copy MCP. Cursor skills also scan ~/.cursor/skills-cursor. Project picker added. Reinstalled app.
- 2026-08-18 · Grok: hang fix — Skills no longer run CompatibilityScanner per project; Usage/Providers load off the main thread; launch scan deferred. Usage now uses Codex rate_limits bars + last-session tokens, not summed totals. Reinstalled /Applications/ProjectHub.app.
- 2026-08-18 · Grok: Claude usage bars now copy CodeBurn: Keychain Claude Code-credentials + GET api.anthropic.com/api/oauth/usage (5-hour + weekly). Refresh every 2 minutes. Reinstalled app.
- 2026-08-18 · Grok: finished remaining primary-provider work. Fixed ProviderCatalog tuple compile. Providers now create/edit skills, enable MCP, and list agents/hooks/plugins. MCP/Settings no longer call CompatibilityScanner on open. Skill inventory restored additional-dir/plugin/disabled/diagnostics without the scanner. Usage cards show remaining % for all 10 primaries. Rebuilt with Xcode Beta and replaced /Applications/ProjectHub.app.
- 2026-08-18 · Grok: Usage no longer touches Keychain or Anthropic. Local JSONL like ccusage: tokens, estimated API $, plan, 5-hour block, 7-day totals. Codex keeps local rate-limit bars. Usage UI is a two-up dashboard plus compact other-provider rows.
- 2026-08-18 · Grok: whole-app chrome is native (sidebar/toolbar/compact header no longer use the dark gradient strip). Added Grok CLI as a primary provider + local usage from ~/.grok/sessions/*/signals.json.
- 2026-08-18 · Grok: sidebar no longer animates whole-page swaps; Compat cache (1.2MB) decodes off the main thread; MCP/Usage/Providers skip repeat work on tab return. Usage leads with remaining quota; dollars are secondary. Desktop uses NavigationSplitView sidebar. Settings is a grouped Form.
- 2026-08-18 · Claude Code: imported the approved Claude Design project (`181de825`) and implemented every page in it — new `HubTheme` token set, `ToolPalette` provider-tile brands, shared `Views/HubComponents.swift`, grouped captioned rail, and screens 2a + 3a–3h. `ContentView.headerGrad` removed app-wide. Repo `DESIGN.md` replaced with the approved canon.
- 2026-08-18 · Claude Code: `swift build` against `/Applications/Xcode.app` fails on an unaccepted licence; `DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift build` works and the redesign compiles clean. Only fix needed was a `@ViewBuilder` lost off `CompatibilityView.projectPicker` during the toolbar rewrite.
- 2026-08-18 · Claude Code: fixed 3 pre-existing test failures (8 assertions), both in Core and unrelated to the redesign. `SkillInventoryReader` compared raw paths while nested discovery resolves symlinks (/var vs /private/var), so subfolder skills were silently marked read-only and skipped by "Copy setup to…"; now canonicalises both sides and scopes mutability to the detected repo root. `MCPReader` keyed dedupe on the provider, so `.mcp.json` listed every server twice (Claude Code + Command Code) for one file; now keyed on the config file. Full suite green: 598 tests, 0 failures.
- 2026-08-18 · Claude Code: built release and replaced `/Applications/ProjectHub.app` with the redesigned build (old bundle backed up to the session scratchpad). Note: worktree and installed bundles share bundle id `com.projecthub.ProjectHubBar`, so `open` on one activates whichever is already running.
- 2026-08-20 · Grok: poteto-mode investigation of perf/quality. Verdict: app is feature-complete; remaining hate is CompatibilityScanner (13775 lines) still used as the plugin+checks engine, CompatibilityView (7688) as UI+scan god object, and Live Mode JSONL walk on MainActor every 2s. Prior WORKLOG already named the scanner split; it was not done. No code change this session.
- 2026-08-20 · Grok: Live Mode explorer confirmed polling is off until Show Live Mode. Extra: 3s snapshot timer runs full ContextEstimator (skills+MCP+CLAUDE.md+JSONL tail) with sidebar closed; INDEX.md async guardrail is violated by sync MainActor pollActiveProject.
- 2026-08-20 · Grok: git-why of scans. Compat is an intentional scan/explain/fix/verify product (agents/BRIEF), not a dump that grew by accident — except 60a8498 which landed the ~13k scanner as a worktree snapshot with no design body. 033b46b locked scans as explicit/single-flight after Skills/Compat freezes. Disk cache is a9367f4; 74f2a53 was in-window only. Scanner split still the open item.
- 2026-08-20 · Grok: SwiftUI explorer verified. ProjectFacts is documented as per-row disk IO (ProjectsView ~927). Skills tab calls installedSkills in body. AgentStore is a pass-through with no @Published. GlobalMCP copy sheet injects empty ProjectStore(). Health tab embeds a second CompatibilityView. Body-time IO is the everyday hitch; scanner split remains the Checks/Plugins cliff.
- 2026-08-20 · Grok: stores/IO explorer: CompatibilityStore.scan has no UI caller; rail badge restore-only. Plugins restore cache on main; Checks decode off-main. Comment Sicko: ~16:1 narrating vs why; do not comment-sweep. Real code: SkillStore/AgentStore empty catches. Swarm complete.
- 2026-08-20 · Grok: behavior-preserving perf/quality pass. CompatibilityScanKind.plugins so Plugins does not inspect MCP; scan-cache keys split; ProjectFacts/tool IDs/installed skills/agents/MCP health/icons cached; ProjectWatcher JSONL walk off MainActor; ContextEstimator reuses static estimate on the 3s timer; empty ProjectStore() on MCP copy fixed; silent skill/agent catches surface as alerts. Pin: testPluginScanSkipsMCPServerInspection + ConfigScanCacheTests + discovery/skill slices green. No comment purge.
- 2026-08-20 · Grok: release-built with Xcode-beta and replaced `/Applications/ProjectHub.app` (v0.3.0, ad-hoc signed) so the perf/quality checkout can be tested. App launched.
- 2026-08-20 · Grok: stopped foreground full-page reloads. Dock/menu-bar open no longer rescans. Skill/project/MCP scans keep the last list on screen and only spin when empty. No NSSound/NSBeep in the app; focus-steal activate skipped when already active.
- 2026-08-20 · Grok: follow-up from Settings/UX/icon investigations. Reopen returns false. Skills/Plugins/MCP/Providers/Usage notes and always-on paths removed. Skills and Checks `for:` includes Global. Bundled provider PNGs for installed apps (Claude, Cursor, Codex, Zed) plus live .app fallback.
- 2026-08-20 · Grok: rechecked marked-done UX. Grouped Claude Code+Desktop as one Providers/MCP family without merging writers. Skills catalog now copies/removes/edits via existing SkillStore APIs, dropped duplicate description + "provider visibility". Bundled Command Code icon. Reinstalled /Applications/ProjectHub.app.
- 2026-09-07 · Claude Code: fixed the tcldb-v4 project-open freeze. Sampled the live hung process (PID 87792): 2287/2287 main-thread samples in `CompatibilityView.body` → `previewableFixes` → `fixPlan` → `disabledCodexSkillOverridePlan` → `ConfigWriter.setCodexSkillOverrideEnabled` → `ProjectRootDetector.detect`. Root cause was an API misuse, not a missing cache: `Project.canonicalize` answers "which project root owns this path" (16.7ms, re-parses ~/.claude.json's 327 keys per call) and three hot sites used it to ask "are these the same file". Against the real `~/.codex/config.toml`, 45 of 61 `[[skills.config]]` sections collapsed onto a shared root, so `matches.count == 1` never held and "Enable skill" silently returned nil for them — and 80cda21's cache refuses to store nils, so each guaranteed nil recomputed every render. Split into `Project.resolvedFilePath` (identity) and `Project.rootOwning` (ownership, renamed so the misuse reads wrong), cached the detector's parsed config behind a stat-based ConfigStamp, and made the fix-plan cache store nils. Measured on the real config: 43.731s → 0.083s for the 29 reachable issues (520x), fix plans 1/29 → 29/29, projected per-render 262.4s → 0.5s (observed hang was 265s). Suite 609 green, runtime 188s → 124s. Deliberately skipped the ~4,100-line FixPlanner extraction all four architect candidates proposed; measurement says it is not needed and the file has zero fix-plan test coverage.
- 2026-09-13 · Command Code: installed 5 project skills into `.claude/skills/` (swiftui-pro, swift-concurrency-pro, swift-testing-pro, swiftdata-pro, swiftui-expert-skill); dropped twostraws nested/plugin wrappers; skill tests green (35/35).
- 2026-09-13 · Command Code: ran the Swift skills (swiftui-pro, swiftui-expert-skill, swift-concurrency-pro, swift-testing-pro) as four review lanes over the tree, then landed only what survived triage. The remote had already shipped `9ec8ac6`, which moves the installed-skill scan off the render path with a cleaner optional-returning `cachedInstalledSkills`; dropped my duplicate of that in favour of theirs. Landed on top: `GlobalMCPView`/`MCPView` built sheet items with `let id = UUID()` inside a binding getter, so identity changed on every body read and SwiftUI could tear the edit/copy sheets down mid-edit and discard typed input — ids now derive from toolID and name; `UsageReader`'s static card cache was written from detached callers unlocked and is now guarded; `ProjectStore.scan()` warms each project's inspection inside its existing detached task instead of calling `invalidateInspectionCache()`, so Projects/ProjectDetail bodies stop re-running ~30 filesystem probes per project on main; `AgentStore` scans off-main and publishes `agentsByPath`; `TextField(onCommit:)` → `.onSubmit`; per-row `RelativeDateTimeFormatter` cached; two `DispatchQueue.main.asyncAfter` timers moved to `Task.sleep`. Test fix: `testWorkspaceMCPNameReservedOnlyForClaudeCode` configured `command = "npx"` and asserted the server was not broken; npx is not installed on every host, so the scanner correctly reported `serverCommandMissing` and the test failed for a reason unrelated to its subject — proved by passing a stub npx on PATH (green with no code change), then switched the fixture to `/bin/echo`. Dismissed with reasons: mass `foregroundColor` sweep (hundreds of churn lines, no user-visible effect), `ForEach(id: \.offset)` over static fixed-order badge rows (no row state to lose), and the runloop-tick `DispatchQueue.main.async` in CompatibilityView (the deferral is load-bearing for render ordering).
- 2026-09-13 · Command Code: brought the "installed in N projects" badge refresh from ~18s to under 1s. Measured first rather than theorising: `Tests/ProjectHubTests/PerfBenchmarkTests.swift` runs against the real home directory and printed the breakdown. Disk was never the problem — `ProjectFacts` costs 0.74 ms/project and the whole 64-project Projects list renders in 23 ms. The cost was that `installedSkills(for:)` did project-independent work once per project: a 105 ms walk of the 145 MB `~/.codex/plugins/cache` (137 dirs, 297 skills) and a 51 ms walk of `~/.claude/skills`, plus a full SKILL.md read per collected skill for its version. Measured at 300 samples: reading SKILL.md cost 1,371 ms while `resolvingSymlinksInPath` cost 4 ms, so the file reads were the target. Added a stamped memo in `SkillInventoryReader` (directory mtime + entry count for roots, market signature for the plugin cache, path-keyed for versions) with `invalidateCaches()` wired into `invalidateInstalledSkills`, which every install/edit/remove path already calls. Result: 18,327 ms → 1,923 ms cold, 1,140 ms warm for 60 projects; the first scan of a project dropped from ~390 ms to ~60 ms. Then added `installedProjectCountsConcurrently`, which scans the first project alone to warm the shared roots before fanning the rest across 8 workers — without the warm-up every worker re-scanned the shared roots at once and the fan-out measured 2.3x SLOWER than serial (6,815 ms vs 2,987 ms), a cache stampede worth remembering. Final: 963 ms cold for 60 projects. Concurrency 16 measured worse than 8, so 8 stays. Not done: `CompatibilityScanner.scan` is still ~2,000 ms and unattributed — the nested skill walk is only 28 ms of it, so something else dominates; that needs its own measurement before anyone quotes a number. 629 tests, 0 failures.
- 2026-09-13 · Command Code: took the Usage tab cold path from 21,294 ms to 6,234 ms. Attributing it first was the whole job: the directory walks are cheap (98 ms `~/.claude/projects`, 161 ms `~/.codex/sessions`, 292 ms local-agent-mode-sessions), and the cost was reading whole session logs — 127 MB across 80 claude files and **368 MB across 80 codex files**, 496 MB per refresh, because `eventsInFile` did `readDataToEndOfFile()` and JSON-parsed every line. For codex that was pure waste: it parsed the entire file and then kept only `result.suffix(1)`, one event. Replaced with a backward chunked scan (256 KB chunks) that walks from the end and stops at the first event older than the 8-day window, stitching the split line at each chunk boundary onto the earlier chunk, with `newestOnly` for codex so it stops at the first hit. Session logs are append-only, so everything in range is at the end. Full suite green at 632. Still NOT under 1 s, and the reason is measured: the 8-day file cutoff means most of the 80 selected files are entirely in-window, so for those the backward scan still reads the whole file — the remaining ~5.5 s is reading claude logs for the week aggregate. The next lever is a persisted per-file (mtime, size) aggregate cache in Application Support so only changed files are re-read, which fixes cold-after-first-run rather than first-ever-run. Also worth noting: `codexCard` walks the sessions tree twice (`loadEvents` plus `latestCodexRateLimits`), and the 4,000-visit cap on a 16 GB tree can truncate before reaching the newest files.
- 2026-09-13 · Command Code: fixed the reported 23 s project-detail tab load. Measuring first killed the wrong theory: the data layer was never the problem — `installedSkills` was 632 ms cold / 38 ms warm and every other tab's loader (ProjectFacts, detectedTools, AgentReader, ConfigReader project scope) was under 2 ms. The cost was the Skills list *rendering* 388 rows eagerly in a plain `VStack`, where each row built a `Menu` containing a button for every tracked project (388 × 22 ≈ 8,500 buttons per render) and re-ran `projectStore.projects.filter` inside the row. The library side was worse: `installed.map { skillStore.isInstalled(...) }` sat inside the loop over 144 globals, roughly 56,000 membership scans per pass. Fixed with `LazyVStack` on both lists, a precomputed `SkillStore.InstalledNameIndex` giving O(1) membership, and one hoisted filter plus one hoisted copy-target list per render instead of per row. Then the deeper question — why walk a 145 MB plugin cache at all. Codex already publishes an index: `~/.codex/config.toml` lists every known plugin as `[plugins."name@market"]` with an `enabled` flag. This machine has 65 cached plugins but only 15 enabled, so the old walk parsed 435 SKILL.md files and reported ~50 disabled plugins as installed. `codexPluginSkills()` now reads that one small file, resolves only enabled plugins to their newest cached version, and falls back to the walk when a config has no plugin section; the plugin-cache stamp now includes config.toml so enabling a plugin invalidates it. Results: `installedSkills` cold 632 ms → 252 ms, plugin skills 297 → 69, installed rows 388 → 160. 633 tests, 0 failures. Not changed on purpose: 153 of the 160 remaining rows are parent-scope skills (from ~/.claude/skills and ~/.codex/skills reached by the walk-up), so the Installed list still shows skills that are not installed in that project — fast now, but that is a product call to narrow to project scope.
- 2026-09-13 · Command Code: audited every page I had previously skipped, on the finding that I had only fixed what was reported. Three parallel audits over the sidebar Skills, Plugins, Projects, MCP, Checks, Providers, Usage, Settings, Live Mode and the rail found the same anti-pattern repeated across seven views, not one. Fixed: sidebar Skills rendered 115 rows in a plain `VStack` and re-derived `deduplicatedGlobalSkills` SEVEN times per body (0.63 ms each) because it is read from seven computed properties; that is now one published `globalSkillGroups` on the store plus a `globalSkillProviderCount`; `GlobalSkillGroup.providerIDs`/`tileIDs` were computed and rebuilt the whole provider catalog per row, now resolved once at construction against a process-wide directory table; per-row menus enumerated every tracked project, now one hoisted array; the `.task(id:)` key built a ~33 KB string with two sorts per render, now a `Hasher`. Projects and Plugins and GlobalMCP and MCP detail lists were all non-lazy `VStack`s, now `LazyVStack`. Plugins recomputed its grouping three times per render with a double-sort and a comparator that re-derived computed status; grouping now runs once and status/components/providerIDs are stored at construction. `MCPStore.serverCount` sorted every server through a comparator that rescanned all tools per comparison, on every rail render — a count never needed the order. `ProjectsView` called `ProjectFacts.configFiles` (10 `fileExists` per tool row) instead of the cached `configFileSummary`. `CompatibilityView` re-read and re-parsed every server's MCP config from disk three times per `body` via `verifiableServers`; now cached per report. Measured: `installedSkills` cold 632 ms → 147 ms, badge refresh 1138 ms cold / 792 ms parallel. 633 tests, 0 failures. Still open and measured, not guessed: Checks full scan ~2.0 s, Usage cold ~6.2 s, and the audit's finding that `CompatibilityScanner.readPlugins` walks the 145 MB plugin cache instead of reading the `[plugins.*]` index the same way `SkillInventoryReader` now does — that is the same 65-vs-15 win and it is the next unit.
- 2026-09-13 · Command Code: fixed skill add/remove and switched installs to symlinks. Reproduced first with `Tests/ProjectHubTests/SkillInstallRemoveTests.swift` rather than guessing, which mattered because the first two hypotheses were wrong. Root cause was a symlink-vs-literal path confusion introduced by the earlier ownership change: `Project.resolvedFilePath` resolves symlinks, so the removal guard followed an installed skill's link out of the project, failed its containment check, and deleted nothing — silently. The same flaw made `canRemove` and `scopeLabel` read from the resolved path, so an installed skill appeared as read-only parent scope with a dead trash button. Fixed by judging ownership on the path as written (`literalPath`) while still deduping on the resolved path, so a link and its target remain one entry. Installs, cross-provider copies and profile copies now symlink instead of duplicating, falling back to a copy when linking fails. This is the pattern already in use on this machine: `~/.claude/skills` holds 39 symlinks pointing at `../../.agents/skills/*`. Measured that content-level duplication was already only 1 skill out of 139 across all roots, so the win is not deduping existing copies but storing one copy per install and having every provider folder reference it — the inventory resolves links when it dedupes, so one skill shows once with several origins. Safety test covers the dangerous case: removing a linked skill deletes the link and asserts the canonical SKILL.md is still on disk, because deleting the resolved path would erase the user's real skill for every project pointing at it. Dead trash/pencil buttons on non-removable rows now render a lock with the reason instead of a control that does nothing. 637 tests, 0 failures.
- 2026-09-13 · Command Code: made the per-project Skills list say what it means. It showed 160 rows for a project that owns 7, because parent-folder and global skills reached by the walk-up were listed identically to installed ones — which also made the trash button look broken, since those rows are read-only by design. The left pane is now "In this project" with the real count, then a divider and "Also available from parent folders" naming how many are inherited rather than owned. Row order within each group is unchanged. 637 tests, 0 failures.
